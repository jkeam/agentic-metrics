apiVersion: v1
kind: Secret
metadata:
  name: metric-dashboard-secret
  namespace: openlit
stringData:
  DB_HOST: openlit-db.openlit.svc.cluster.local
  DB_PORT: "8123"
  DB_USER: default
  DB_PASSWORD: OPENLIT
  DYNAMIC_CHART_PATH: /opt/app-root/charts
